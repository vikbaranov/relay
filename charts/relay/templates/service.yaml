apiVersion: v1
kind: Service
metadata:
  name: {{ include "relay.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "relay.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "relay.selectorLabels" . | nindent 4 }}
  ports:
    - name: health
      port: 8080
      targetPort: health
      protocol: TCP
    - name: webhook
      port: {{ .Values.config.lifecycle.webhookHostPort }}
      targetPort: webhook
      protocol: TCP
