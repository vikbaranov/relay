apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "relay.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "relay.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "relay.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "relay.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
