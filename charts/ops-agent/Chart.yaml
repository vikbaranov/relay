apiVersion: v2
name: ops-agent
description: Helm chart for the ops-agent relay controller — a Mattermost AI bot that manages per-user ZeroClaw sandbox pods in Kubernetes
type: application
version: 0.1.0
appVersion: "0.4.1"
keywords:
  - mattermost
  - ai
  - bot
  - kubernetes
sources:
  - https://github.com/vbaranov/ops-agent
